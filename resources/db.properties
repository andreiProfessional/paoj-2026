# SQLite — no server needed; the file is created automatically next to the project root.
# Change the path if you want the DB stored elsewhere.
db.url=jdbc:sqlite:banking_app.db
db.user=
db.password=
